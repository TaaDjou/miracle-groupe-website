# Relative path - Vite proxies /api to the backend (see vite.config.js), so this
# works both in the browser dev tools pane (no cross-origin request needed) and
# in a production build served behind a reverse proxy that forwards /api the same way.
VITE_API_URL=/api
